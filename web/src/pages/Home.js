import React from "react";
import { Button } from "../shared/components/uikits";

const Home = () => {
  return (
    <Button type="primary" size="large">
      Submit
    </Button>
  );
};

export default Home;
